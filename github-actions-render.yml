name: Keep render awake
on: 
  schedule:
    - cron: "*/11 * * * *"
    
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-slim
    steps:
      - name: Ping Render
        run: curl -fsS --max-time 30 https://banksim-webswing-0-1.onrender.com/ > /dev/null \
        && echo "Ok"
